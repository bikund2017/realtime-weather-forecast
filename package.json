{
  "name": "weather-forecast-app",
  "version": "1.0.0",
  "description": "Full-stack weather forecast application",
  "scripts": {
    "build": "echo 'Frontend is static, no build needed'",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0"
  }
}
